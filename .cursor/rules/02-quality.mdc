---
description: 交付前品質檢查（🟡🔴 完成時、/review 時、實作驗證時套用）
---

# 品質檢查

🟢 簡單任務：快速確認即可。🟡🔴 任務完成前對照下列清單。

## 基本

- [ ] 符合使用者需求
- [ ] 無 syntax / linter 錯誤（有跑過相關檢查時）
- [ ] 未提交 secrets、API keys

## 程式品質（有改 code 時）

- [ ] 命名清楚、無明顯重複
- [ ] 邊界與錯誤情況有處理
- [ ] 改動範圍最小，未動到無關檔案

## 測試（有測試框架且改了行為時）

- [ ] 相關測試已執行且通過
- [ ] 新行為有對應測試（若專案慣例要求 TDD）

## workflow-state 檢查（強制，🟢 單步豁免除外）

- [ ] 有實作步驟：已寫入 `workflow-state.json` 且 status 正確？
- [ ] 開始前 `IN_PROGRESS`、完成後 `ALL_DONE`？
- [ ] todos 全部 `completed` 或合理 `cancelled`？
- [ ] 或明確 `NEED_REVISION` + `revision` 原因

## smart_think 檢查

- [ ] 🔴：THINK 與 REVIEW 皆已執行（smart_think 或 inline 降級）？
- [ ] 🟡：若命中觸發條件，已執行？
- [ ] `/think` 或使用者要求分析時，已結構化分析？

## TodoWrite 同步（有使用時）

- [ ] TodoWrite 與 workflow-state.json 的 todos 一致？
- [ ] 無遺留 `in_progress` / `pending`（除非任務仍在進行）？

## 常見問題

- **跳過 workflow-state 直接改** → 🟡🔴 **禁止**；🟢 單步豁免除外
- **ALL_DONE 但 todos 未完成** → Stop Hook 會追問
- **迭代停不下來** → 確認 status 為 `ALL_DONE` 且 todos 已完成；或已達 3 輪上限
- **TodoWrite 與 JSON 不同步** → 以 workflow-state.json 為準，立即同步
