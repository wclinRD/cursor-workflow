---
description: 複雜度路由規則，根據任務複雜度選擇適當的處理方式
alwaysApply: true
---

# 複雜度路由規則

> 根據任務複雜度選擇適當的處理方式

---

## 🟢 簡單任務（≤ 3 分）

### 特徵
- 步驟 ≤ 2
- 工具 ≤ 1
- 檔案 ≤ 1
- 低風險

### 處理方式
1. 直接執行，不需完整迴圈
2. 用 smart_think 快速分析
3. 用 smart_fast_apply 直接修改
4. 簡單驗證

### 範例
- 修改一個函式的參數
- 更變數名
- 修正簡單的語法錯誤

---

## 🟡 中等任務（4-8 分）

### 特徵
- 步驟 3-4
- 工具 2-3
- 檔案 2-3
- 中風險

### 處理方式
1. 完整 think → plan → execute → review 迴圈
2. 用 smart_think 結構化分析
3. 寫入 scratchpad.md 追蹤
4. 逐項執行，即時更新
5. 遵循 TDD 原則（先寫測試）

### 範例
- 新增一個 API 端點
- 重構一個模組
- 修一個複雜的 bug

---

## 🔴 複雜任務（> 8 分）

### 特徵
- 步驟 > 4
- 工具 > 3
- 檔案 > 3
- 高風險

### 處理方式
1. 完整 think → plan → execute → review 迴圈
2. 用 smart_think 進行深度分析
3. 考慮使用 subagent（如有）
4. 多輪迭代，每次专注小範圍
5. 充分測試（TDD 原則）
6. 使用 Plan Mode 建立詳細計畫

### 範例
- 大規模重構
- 新功能開發（多模組）
- 複雜的 debug 流程

---

## 📊 評分公式

```
複雜度分數 = 步驟×2 + 工具×2 + 檔案×1 + 風險×3

≤ 3  → 🟢 簡單
4-8  → 🟡 中等
> 8  → 🔴 複雜
```

### 風險評分
- 低風險 = 1
- 中風險 = 2
- 高風險 = 3（涉及認證、金流、資料庫 schema 等）

---

## 🔄 動態調整

如果在執行過程中發現：
- 比預期簡單 → 降級處理
- 比預期複雜 → 升級處理，重新分析
- 遇到阻塞 → 標記 blocked，考慮繞過或暫停

---

## 🎯 複雜度與工具選擇

| 複雜度 | 建議工具 | 迭代次數 |
|--------|---------|---------|
| 🟢 簡單 | smart_think (cit) + smart_fast_apply | 1 輪 |
| 🟡 中等 | smart_think (structured) + scratchpad.md | 1-2 輪 |
| 🔴 複雜 | smart_think (beam/forest) + Plan Mode + TDD | 2-3 輪 |
