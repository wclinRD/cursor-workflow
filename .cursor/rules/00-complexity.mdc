---
description: 依任務複雜度路由處理方式（🟢 直接做 / 🟡 標準流程 / 🔴 完整迴圈）
alwaysApply: true
---

# 複雜度路由

收到任務後，先快速評分再選流程。不要對所有任務套用同一套 heavyweight 流程。

## 評分公式

```
分數 = 步驟×2 + 工具×2 + 檔案×1 + 風險×3

≤ 3  → 🟢 簡單
4-8  → 🟡 中等
> 8  → 🔴 複雜
```

風險：低=1、中=2、高=3（認證、金流、schema 變更等）

## TodoWrite 使用規則（強制）

| 類型 | TodoWrite |
|------|-----------|
| 純問答 / 解釋 code（無實作） | 可跳過 |
| 🟢 有實作步驟 | **必須**（至少 1 項） |
| 🟡 / 🔴 | **必須**（開始實作前建立） |
| `/plan` | **必須** |

**不可**在沒有 TodoWrite 的情況下開始改 code 或跑多步驟任務（純問答除外）。

## smart_think 使用規則

| 複雜度 | smart_think |
|--------|-------------|
| 🟢 | **不用** |
| 🟡 | **符合觸發條件才用**（見 `01-workflow-hybrid.mdc`） |
| 🔴 | **必須**（THINK 與 REVIEW 階段） |
| `/think` | **強制** |

## 🟢 簡單（≤ 3 分）

- 純問答：直接回答，跳過 TodoWrite
- **有實作**：**TodoWrite**（1–2 項）→ 執行 → 標記 completed
- 跳過 smart_think 與完整 REVIEW 迴圈
- 用 Read / Grep / StrReplace 即可
- 範例：改 typo、重命名、解釋一段 code

## 🟡 中等（4-8 分）

- **必須 TodoWrite**：分析後、實作前建立子任務
- 標準流程：分析 → TodoWrite → 實作 → 驗證 → REVIEW
- 同時只有一項 `in_progress`；完成一項立即 `merge: true` 更新
- 涉及新行為時遵循 TDD
- **smart_think**：符合觸發條件時必須呼叫
- 範例：新增 API、模組重構、跨 2-3 檔案的 bug

## 🔴 複雜（> 8 分）

- **必須 TodoWrite**：smart_think 之後、實作前建立
- **必須 smart_think**：THINK 與 REVIEW 階段
- 完整 Hybrid 迴圈（見 `01-workflow-hybrid.mdc`）
- `workflow-status.md` → `IN_PROGRESS`；完成 → `ALL_DONE`
- 範例：大重構、多模組新功能、難以重現的 debug

## 動態調整

- 執行中發現比預期簡單 → 可合併 TodoWrite 項，但**不可完全取消**（除非變純問答）
- 執行中發現比預期複雜 → 升級，**補 TodoWrite 項** + smart_think + 🔴 流程
- 純問答、讀 repo → 可跳過 TodoWrite
