---
description: 依任務複雜度路由處理方式（🟢 直接做 / 🟡 標準流程 / 🔴 完整迴圈）
alwaysApply: true
---

# 複雜度路由

收到任務後，先快速評分再選流程。不要對所有任務套用同一套 heavyweight 流程。

## 評分公式

```
分數 = 步驟×2 + 工具×2 + 檔案×1 + 風險×3

≤ 3  → 🟢 簡單
4-8  → 🟡 中等
> 8  → 🔴 複雜
```

風險：低=1、中=2、高=3（認證、金流、schema 變更等）

> workflow-state、smart_think 詳細規則見 `01-workflow-hybrid.mdc`。

## 🟢 簡單（≤ 3 分）

- 純問答：直接回答，跳過追蹤，維持 `IDLE`
- **🟢 單步豁免**（1 步、1 檔、低風險）：直接實作，**可不寫** workflow-state.json
- **🟢 多步或有實作**：workflow-state.json → `IN_PROGRESS` → 執行 → `ALL_DONE`
- 跳過 smart_think 與完整 REVIEW 迴圈
- 範例：改 typo（單步豁免）、重命名單一符號（單步豁免）

## 🟡 中等（4-8 分）

- **必須** workflow-state.json（實作前建立 todos + status）
- 標準流程：分析 → workflow-state → `IN_PROGRESS` → 實作 → 驗證 → REVIEW → `ALL_DONE`
- 涉及新行為時遵循 TDD
- smart_think：符合觸發條件時必須（見 `01-workflow-hybrid.mdc`）
- 範例：新增 API、模組重構、跨 2-3 檔案的 bug

## 🔴 複雜（> 8 分）

- **必須** smart_think THINK → workflow-state → 實作 → smart_think REVIEW
- **必須** workflow-state.json（Stop Hook 最多 3 輪迭代）
- 完整 Hybrid 迴圈（見 `01-workflow-hybrid.mdc`）
- 範例：大重構、多模組新功能、難以重現的 debug

## 動態調整

- 執行中發現比預期簡單 → 若符合 🟢 單步豁免可簡化；否則合併 todos
- 執行中發現比預期複雜 → 升級，**補 todos** + smart_think + 🔴 流程
- 純問答、讀 repo → 跳過 workflow-state（維持 `IDLE`）
