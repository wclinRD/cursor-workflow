---
description: 🟡🔴 任務的 THINK → PLAN → EXECUTE → REVIEW 流程（🟢 可跳過）
alwaysApply: true
---

# Hybrid 工作流

> 任務追蹤 **一律強制 TodoWrite + workflow-status**（純問答除外）。
> smart_think 依複雜度與觸發條件決定（見下）。

## TodoWrite（強制）

### 何時必須建立
- 任何涉及 **改 code、跑命令、多步驟實作** 的任務
- `/plan`、`/think`（若後續要實作）、🟡、🔴 任務
- **實作前** 建立，不可先改檔再補 todo

### 何時可跳過
- 純問答：解釋 code、回答問題、讀 repo 說明
- 單句可完成的 🟢 且使用者未要求追蹤

### 使用規則
1. 建立後立即將第一項設為 `in_progress`
2. **同時只有一項** `in_progress`
3. 完成驗證後才標 `completed`
4. 用 `merge: true` 更新，不要整份重建
5. 任務結束前所有項須 `completed` 或 `cancelled`
6. 🟢 至少 1 項；🟡🔴 依子任務拆分（通常 2+ 項）

---

## workflow-status（強制）

### 何時必須更新
- 任何涉及 **改 code、跑命令、多步驟實作** 的任務
- `/plan`、`/think`（若後續要實作）、🟢、🟡、🔴 任務
- **實作前** 設為 `IN_PROGRESS`，不可先改檔再補

### 何時維持 IDLE
- 純問答：解釋 code、回答問題、讀 repo 說明

### 狀態值
| 狀態 | 時機 |
|------|------|
| `IDLE` | 無進行中任務（預設） |
| `IN_PROGRESS` | 開始實作前 |
| `ALL_DONE` | 所有子任務完成且驗證通過 |
| `NEED_REVISION: [原因]` | 審查發現需修正 |

Stop Hook 讀取此檔決定是否自動迭代（最多 3 輪）。

---

## smart_think 決策

### 🟢 — 不用 smart_think
TodoWrite（若有實作）→ workflow-status → `IN_PROGRESS` → Read / Grep / StrReplace / Shell → `ALL_DONE`。

### 🟡 — 符合以下任一條件 **必須** 呼叫 `smart_think`

1. **需求模糊**：目標不清、有多種可行做法、需 trade-off
2. **跨檔案依賴**：涉及 3+ 檔案或模組邊界
3. **根因不明**： bug 現象清楚但原因未確認
4. **架構取捨**：影響 API 設計、資料流、擴充性
5. **使用者要求**：明確說「分析」「先想」「/think」
6. **執行中卡關**：第一次嘗試失敗，需重新拆解

**不符合以上** → inline 簡短分析即可。

### 🔴 — **必須** smart_think

| 階段 | 工具 |
|------|------|
| THINK | `smart_think`（structured）或 `smart_deep_think` |
| REVIEW | 至少 `smart_think`；高風險用 `smart_deep_think` |

🔴 順序：**smart_think THINK → TodoWrite → 實作**（THINK 前不得改 code 或建 TodoWrite）。

---

## 標準流程（🟡）

```
分析 [smart_think?] → TodoWrite（必須）→ workflow-status IN_PROGRESS → 實作 → 驗證 → REVIEW → ALL_DONE
```

1. **分析**：評估 🟡 smart_think 觸發條件
2. **TodoWrite**：**必須**建立子任務
3. **實作**：逐項完成，每項完成即更新 todo
4. **驗證**：Shell 測試或手動確認
5. **REVIEW**：TodoWrite 全部 `completed`

## 完整迴圈（🔴）

```
smart_think → TodoWrite（必須）→ workflow-status IN_PROGRESS → [TDD] → IMPLEMENT → VERIFY → smart_think REVIEW → ALL_DONE
```

- TodoWrite 在 THINK 之後、實作之前 **必須** 建立
- Stop Hook 最多 3 輪

## 手動觸發

| 指令 | TodoWrite | smart_think |
|------|-----------|-------------|
| `/think` | 若後續實作則必須 | 強制 |
| `/plan` | **必須** | 🔴 必須；🟡 看觸發條件 |
| `/review` | 檢查全部 completed | 🔴 必須 |

## 工具優先順序

- **追蹤**：TodoWrite + workflow-status（強制）
- **思考**：smart_think / smart_deep_think（依上表）
- **讀寫搜尋測試**：Cursor built-ins（見 `smart-mcp.mdc`）

## Context 管理

- 切換任務、Agent 混淆、完成一個功能單元 → 考慮新對話
- 長對話可用 `smart_compact`（若可用）
