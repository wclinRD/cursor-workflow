---
description: Smart MCP 工具規則，確保正確使用 Smart MCP 工具
globs: **/*.{ts,tsx,js,jsx,py,rs,go}
alwaysApply: false
---

# Smart MCP 工具規則

> 確保正確使用 Smart MCP 工具

---

## 🥇 一級工具（優先使用）

### 讀取類
- `smart_read` — 取代原生 read
  - `auto` — 自動判斷讀取方式
  - `outline` — 僅顯示結構
  - `signatures` — 顯示函式簽章
  - `symbol` — 提取特定符號
  - `explain` — 符號 + imports + callers
  - `range` — 特定行數範圍
  - `full` — 完整讀取

### 搜尋類
- `smart_grep` — 取代原生 grep
  - 支援 regex + scope + budget
  - 自動壓縮結果

### 編輯類
- `smart_fast_apply` — 取代原生 edit
  - `search-replace` — 搜尋替換
  - `unified-diff` — diff 格式（最省 token）
  - `hashline` — 行號定位（大檔案）
  - `block-diff` — 符號級編輯

### 思考類
- `smart_think` — 結構化思考
  - `cit` — 自動分支（預設）
  - `beam` — 多路徑探索
  - `forest` — 多樹共識
  - `structured` — 語法約束 CoT

### 網路類
- `smart_exa_search` — 網路搜尋
  - 必須加 `compress: "caveman"`
  - 必須加 `compressLevel: "semantic"`

### 測試類
- `smart_test` — 執行測試
  - 自動偵測測試框架（vitest/jest/mocha）
  - 支援 watch mode、coverage
  - `related` — 找出相關測試

---

## 🔴 禁止使用

- ❌ 原生 `read` — 用 `smart_read` 取代
- ❌ 原生 `grep` — 用 `smart_grep` 取代
- ❌ 原生 `webfetch` — 用 `smart_exa_crawl` 取代
- ❌ 不加壓縮的搜尋 — 必須加 compress 參數

---

## 📦 搜尋結果管理

```
結果 < 20KB  → 直接使用
結果 20-50KB → 先摘要再整合
結果 > 50KB  → 降低 numResults 或加重 compressLevel
```

---

## 🔧 Cursor 原生工具

### Plan Mode
- 按 `Shift+Tab` 切換 Plan Mode
- Agent 會先研究 codebase、問問題、建立計畫
- 計畫會以 Markdown 格式開啟，可直接編輯
- 點擊「Save to workspace」儲存到 `.cursor/plans/`

### @Branch
- 使用 `@Branch` 提供 branch context
- 範例：「Review the changes on this branch」
- 範例：「What am I working on?」

### Context 管理
- 不需要手動標籤每個檔案
- Agent 會自動搜尋相關檔案
- 使用 `@` 標籤特定檔案（如已知確切路徑）
- 避免包含不相關的檔案（會混淆 Agent）
